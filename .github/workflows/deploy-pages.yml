name: Deploy website

on:
  push:
    branches: [main]
  workflow_dispatch:
  workflow_run:
    workflows: ["🧠 Update Publications from ORCID"]
    branches: [main]
    types: [completed]

permissions:
  contents: read

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Check out lab website
        uses: actions/checkout@v4
        with:
          ref: main
          path: lab
          persist-credentials: false
      - name: Assemble public files
        run: python3 lab/scripts/build_pages.py --main lab --output _site
      - name: Upload website
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Publish website
        id: deployment
        uses: actions/deploy-pages@v4
